# API Requirements
flask==3.0.0
flask-cors==4.0.0

# Core requirements (if not already installed)
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
pretty_midi>=0.2.10
tqdm>=4.65.0

# Optional: Audio conversion
midi2audio>=0.1.1
pydub>=0.25.1
